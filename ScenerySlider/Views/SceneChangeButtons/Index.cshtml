@model IEnumerable<ScenerySlider.Models.SceneChangeButton>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Scene.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PositionX)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PositionY)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Scene.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PositionX)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PositionY)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
